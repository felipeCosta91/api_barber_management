const { resolve } = require('path') //padroniza os caminhos tanto no windows quanto no mac

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'), //caminho configurações banco
    'models-path': resolve(__dirname, 'src', 'App', 'models'),  //caminho para os models 
    'migrations-path': resolve(__dirname,'src','database','migrations'),//caminho para as migrations
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds') //caminho para os seeds
    
    
}